const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "root",
      password: "c7Qc#Tk5wMYBdn:iByFm",
      database: "inventory",
    }
  };
  module.exports = config;